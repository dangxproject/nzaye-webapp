{
  "name": "nzaye",
  "description": "NZ, aye! RSS feed reader for news around New Zealand, built on top of React.js",
  "version": "1.0.0",
  "author": "Richard Sentino <richard@mindginative.com>",
  "dependencies": {
    "gatsby": "^1.8.10",
    "gatsby-link": "^1.6.5",
    "gatsby-plugin-react-helmet": "^1.0.1"
  },
  "keywords": [
    "gatsby",
    "react"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^1.5.2"
  }
}
